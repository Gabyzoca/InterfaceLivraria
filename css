/* Cores usadas */
:root {
  --cor-vinho: #a52a2a;       /* Tom de vinho */
  --cor-marrom-escuro: #543d35; /* Marrom mais escuro */
  --cor-marrom-medio: #8b4513;  /* Marrom médio */
  --cor-bege-claro: #f5f5dc;   /* Bege claro */
  --cor-bege-medio: #deb887;   /* Bege médio */
  --cor-texto-escuro: #333;    /* Cor de texto escura geral */
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: var(--cor-bege-claro); /* Fundo geral bege claro */
  color: var(--cor-texto-escuro);        /* Cor do texto padrão */
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/*cabeçalho*/
.cabecalho {
  background-color: var(--cor-marrom-escuro) !important; /* Fundo marrom escuro */
}

.texto-logo {
  color: var(--cor-bege-claro) !important; /* Texto do logo bege claro */
  text-decoration: none; 
  font-size: 2rem; 
  margin-left: 1rem; 
}

.texto-logo:hover {
  color: var(--cor-bege-claro) !important; 
}

header .campo-texto {
  background-color: #fffaf0; 
  border: 1px solid var(--cor-marrom-medio);
  color: var(--cor-texto-escuro);
}

header .botao {
  color: var(--cor-bege-claro);
  border-color: var(--cor-bege-claro);
}

header .botao:hover {
  background-color: var(--cor-bege-claro);
  color: var(--cor-marrom-escuro);
  border-color: var(--cor-bege-claro);
}

header a.link {
  text-decoration: none;

.texto-direita {
  text-align: end;
}

.linha-flex {
  display: flex;
  align-items: center;
}

.area-logo {
  flex: 1; 

.area-busca {
  flex: 2; 
  margin: 0 1rem;
}

.area-usuario {
  flex: 1;
  text-align: right;
}


.botao-usuario,
.botao-carrinho {
  background: none;
  border: none;
  padding: 0;
  margin: 0;
  cursor: pointer;
  color: var(--cor-bege-claro); 
  transition: color 0.3s ease, background-color 0.3s ease; 
}

.botao-usuario:hover,
.botao-carrinho:hover {
  color: var(--cor-vinho) !important; /
 
}


.menu {
  background-color: var(--cor-bege-medio) !important;
}

.lista-menu .link {
  color: var(--cor-marrom-escuro) !important; 
}

.lista-menu .link:hover {
  color: var(--cor-vinho) !important; 
}

.item {
  list-style: none;
}


.principal {
  margin-top: 2rem;
}

.carrossel {
  margin-bottom: 2rem;
}

.indicadores button {
  background-color: var(--cor-marrom-medio);
  border: none;
  border-radius: 50%;
  width: 10px;
  height: 10px;
  margin: 0 5px;
  opacity: 0.5;
}

.indicadores .ativo {
  opacity: 1;
}

.slides .slide {
  height: 400px;
  background-color: var(--cor-bege-claro);
  display: flex;
  justify-content: center;
  align-items: center;
}

.imagem-banner {
  max-height: 400px;
  object-fit: cover;
  border-radius: 8px;
}

.legenda {
  background-color: rgba(0, 0, 0, 0.5);
  color: var(--cor-bege-claro);
  padding: 1rem;
  bottom: 0;
  left: 0;
  right: 0;
}

.bg-marrom-transparente{
  background-color: rgba(98, 49, 8, 0.7);
}


#carrossel-destaques {
  margin-top: 2rem;
  margin-bottom: 2rem;
}

.livro-carrossel {
  display: flex;
  flex-direction: column; 
  align-items: center;
  text-align: center;
}

.livro-carrossel img {
  max-height: 300px; 
  object-fit: contain; 
  margin-bottom: 1rem; 
  border-radius: 8px; 
}

.legenda-carrossel {
  background-color: rgba(0, 0, 0, 0.5);
  color: var(--cor-bege-claro);
  padding: 1rem;
  border-radius: 8px;
  width: 80%; 
  margin-top: 1rem;
}

.legenda-carrossel h5 {
  margin-bottom: 0.5rem;
}

/* Estilos para o rodapé */
.rodape {
  background-color: var(--cor-marrom-escuro) !important; 
}

.texto-bege {
  color: var(--cor-bege-claro) !important; 
  text-decoration: none; 
}

.link:hover {
  color: var(--cor-vinho) !important; 
}

.social {
  margin-top: 0.5rem;
}

.texto-rodape {
  margin-bottom: 0;
}
